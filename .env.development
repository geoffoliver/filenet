# .env.development
# The host is derived at runtime from window.location (see
# app/lib/api.ts's apiUrl) so the dev server is reachable from another
# machine on the network — only the API server's port is fixed here.
NEXT_PUBLIC_DEV_API_PORT=3000
